.DS_Store
node_modules/
dist/
.turbo/
# Prisma 7 prisma-client generator output (regenerated via postinstall)
apps/api/src/generated/
.env
.env.local
*.log
# Visual brainstorming companion mockups (transient)
.superpowers/brainstorm/

# Superpowers brainstorm/SDD scratch
.superpowers/

# Dumps do banco (contem dados de saude de pacientes — nunca versionar)
/backups/

# Ad creatives at the repo root only. Keep the leading slash: an unanchored
# `marketing/` also matches apps/web/src/components/marketing, and Tailwind v4
# skips gitignored paths when detecting class names.
/marketing/

.worktrees/
