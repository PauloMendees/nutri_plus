# Marketing screenshots (optional)

Drop real product screenshots here to replace the UI mockups on the landing page:

- `hero-dashboard.png` — web dashboard / meal plan editor (≈1600×1000)
- `patient-app.png` — patient mobile app (≈800×1600)
- `silhueta.png` — Silhueta report (≈1200×900)

PNG or WebP. Until these files exist, the landing uses branded React mockups.

After adding files, set the matching path in `src/components/marketing/marketing-assets.ts`.
