# @nutri-plus/web

Web client for nutri_plus (Next.js App Router). Part of the pnpm/Turbo monorepo.

## Develop

```bash
pnpm --filter @nutri-plus/web dev   # http://localhost:3001
```

Requires `apps/web/.env.local` — see `.env.example`.
